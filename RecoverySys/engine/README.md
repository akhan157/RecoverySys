# Deferred engine research

The production simulation authority is the browser JavaScript implementation in
`src/lib/simulation.js`. Python code and experiments here are research/deferred-
only and are not a second runtime engine.
