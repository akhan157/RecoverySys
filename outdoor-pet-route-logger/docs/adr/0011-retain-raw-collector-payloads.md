# Retain Raw Collector Payloads

Collector output will be stored locally as raw payloads alongside normalized fields. This makes the collector spike auditable and lets the schema/parser evolve when Find Hub, Android automation, or browser scraping exposes different fields.
