# Manual Session Control

Outside sessions begin and end only when the user explicitly starts or stops them. Schedule-based and geofence-based detection are deferred because they depend on routines or tracker reliability that the first version is specifically trying to measure.
