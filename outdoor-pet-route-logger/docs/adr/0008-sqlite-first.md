# SQLite First

The first persistence layer will be SQLite. Reliability analysis needs structured queries across outside sessions, check attempts, location points, stale age, accuracy/confidence, repeated observations, and failure reasons; JSONL or CSV would become awkward once the app needs daily summaries and comparisons.
