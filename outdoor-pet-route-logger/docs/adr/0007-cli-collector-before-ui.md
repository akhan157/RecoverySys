# CLI Collector Before UI

The first real build will be a CLI/data collector, not a UI app. This keeps the project focused on proving Find Hub / Moto Tag data access, persistence, and reliability summaries before spending effort on maps or interaction design.
