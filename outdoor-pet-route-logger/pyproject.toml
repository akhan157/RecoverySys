[project]
name = "outdoor-pet-route-logger"
version = "0.1.0"
description = "Local CLI for auditing Moto Tag / Find Hub outdoor pet location reliability."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
petlog = "petlog.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
